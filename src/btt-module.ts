/**
 * Entry for the copy of this library that ships INSIDE BetterTouchTool (Run Real JavaScript:
 * `const { Btt, actions } = require("bettertouchtool")`). Everything from the main entry plus the
 * catalog under `catalog`.
 */
export * from "./index.js";
export * as catalog from "./catalog.js";
